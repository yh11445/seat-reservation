<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.samickthk.seatreservation.mapper.SeatMapper">

	<resultMap type="com.samickthk.seatreservation.domain.SeatReservation" id="seatReservation">
		<result property="seatId" column="seat_id"/>
		<result property="seatName" column="seat_nm"/>
		<result property="userId" column="user_id"/>
		<result property="userName" column="name"/>
	</resultMap>

	<select id="selectSeatReservationInfos" resultMap="seatReservation">
		SELECT
			A.seat_id,
			A.seat_nm,
			B.user_id,
			C.name
			
		FROM
			seat A
			LEFT JOIN seat_rsvn B
			ON A.seat_id = B.seat_id
			AND B.rsvn_date = #{date}
			LEFT JOIN user C
			ON B.user_id = C.user_id
			
		ORDER BY A.seat_id
	</select>

	<insert id="insertSeatReservationToday">
		INSERT INTO seat_rsvn (
			user_id,
			seat_id,
			rsvn_date
		) VALUES (
			#{userId},
			#{seatId},
			CURDATE()
		)
	</insert>
	
	<select id="selectSeatReservationCountTodayBySeatId" resultType="int">
		SELECT COUNT(*)
		FROM seat_rsvn
		WHERE rsvn_date = CURDATE()
		AND seat_id = #{seatId}
	</select>

	<delete id="deleteSeatReservationTodayByUserId">
		DELETE FROM seat_rsvn
		WHERE rsvn_date = CURDATE()
		AND user_id = #{userId}
	</delete>
	
	<select id="selectSeatReservationCountTodayByUserId" resultType="int">
		SELECT COUNT(*)
		FROM seat_rsvn
		WHERE rsvn_date = CURDATE()
		AND user_id = #{userId}
	</select>
	
	<select id="selectSeatReservationInfoByUserId" resultMap="seatReservation">
		SELECT
			A.seat_id,
			A.seat_nm,
			B.user_id,
			C.name
			
		FROM
			seat A,
			seat_rsvn B,
			user C
			
		WHERE 1=1
		AND A.seat_id = B.seat_id
		AND B.user_id = C.user_id
		AND B.user_id = #{userId}
		AND B.rsvn_date = #{date}
	</select>

</mapper>